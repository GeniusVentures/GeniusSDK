---
phase: 02-verification-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-verification-documentation/02-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03

must_haves:
  truths:
    - "All 3 init functions return non-null path string on success"
    - "All 3 init functions return nullptr on every failure path"
    - "Zero references to old factory methods (NewFromPrivateKey, NewFromMnemonic) exist in GeniusSDK.cpp"
    - "Zero references to old multi-param New(autodht, ...) exist in GeniusSDK.cpp"
    - "Every @param tag in init function Doxygen blocks matches the actual function signature"
    - "No @param tags reference removed parameters (autodht, process, baseport, is_full_node)"
    - "A formal verification report exists in 02-VERIFICATION.md"
  artifacts:
    - path: ".planning/phases/02-verification-documentation/02-VERIFICATION.md"
      provides: "Formal verification report with PASS/FAIL for each VER requirement"
      min_lines: 60
  key_links:
    - from: "02-VERIFICATION.md"
      to: "src/GeniusSDK.cpp"
      via: "grep evidence for VER-01 and VER-02 checks"
    - from: "02-VERIFICATION.md"
      to: "src/GeniusSDK.h"
      via: "@param tag comparison for VER-03 check"
---

<objective>
Formally verify that the Phase 1 refactoring preserves its return contract, contains zero dead references, and has accurate Doxygen documentation. Produce a structured verification report (02-VERIFICATION.md) that documents every check and its outcome.

Purpose: Lock in Phase 1 correctness before closing the milestone — prove the init interface behaves as specified, not just that files were modified. Verification is contractual (code paths, dead references, doc accuracy), not structural.

Output: A single verification report plus updated planning state.
</objective>

<execution_context>
@/Users/henriqueklein/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/henriqueklein/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/GeniusSDK.h
@src/GeniusSDK.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: VER-01 Return Contract Verification</name>
  <files>
    - .planning/phases/02-verification-documentation/02-VERIFICATION.md (created)
  </files>
  <action>
Verify VER-01 — all 3 init functions return non-null path string on success and nullptr on every failure path.

Run these checks and record results in a new 02-VERIFICATION.md file:

**Check A — GeniusSDKInit (lines 166-186):**
Trace every return path:
- L171: `!base_path || !dev_config || dev_config[0] == '\0'` → `return nullptr` (bad params)
- L177: `!cfg` (ParseDevConfig fails) → `return nullptr` (parse error)
- L181: `!GeniusNodeInstance` (GeniusNode::New fails) → `return nullptr` (null node)
- L185: success path → `return ret_val.c_str()` where `ret_val` = `"Initialized on " + base_path` (non-null)
Record: PASS if all 4 paths covered, FAIL if any path misses null return or success path doesn't return a non-null string.

**Check B — GeniusSDKInitWithKey (lines 188-214):**
Trace every return path:
- L193: `!base_path || !dev_config || dev_config[0] == '\0'` → `return nullptr`
- L198: `!eth_private_key || eth_private_key[0] == '\0'` → `return nullptr`
- L204: `!cfg` → `return nullptr`
- L209: `!GeniusNodeInstance` → `return nullptr`
- L213: success path → `return ret_val.c_str()` (non-null)
Record: PASS if all 5 paths covered, FAIL otherwise.

**Check C — GeniusSDKInitWithMnemonic (lines 216-242):**
Trace every return path:
- L221: `!base_path || !dev_config || dev_config[0] == '\0'` → `return nullptr`
- L226: `!mnemonic || mnemonic[0] == '\0'` → `return nullptr`
- L232: `!cfg` → `return nullptr`
- L237: `!GeniusNodeInstance` → `return nullptr`
- L241: success path → `return ret_val.c_str()` (non-null)
Record: PASS if all 5 paths covered, FAIL otherwise.

Also confirm the std::string captures (key_copy at L206, mnemonic_copy at L234) prevent use-after-free — the AccountSource variant holds a std::string copy, not a raw pointer to the C string parameter. This validates IMPL-06 from Phase 1 is preserved.

**Check D — Cross-function:**
- Verify `ret_val` is `static std::string` with `.assign()` reset (no cross-call accumulation)
- Confirm no function returns a dangling pointer to a stack local

For each check, record in the verification report: check ID, requirement, PASS/FAIL, evidence (line numbers, grep output snippets).
</action>
  <verify>
    <automated>
rg -n 'return nullptr|return ret_val' src/GeniusSDK.cpp  # confirm 14 total return sites (9 nullptr + 3 success + 2 other)
rg -c 'return nullptr' src/GeniusSDK.cpp  # within init functions: should find 8 (2+3+3)
rg -n 'static std::string ret_val' src/GeniusSDK.cpp  # confirm static declaration
rg -n 'ret_val\.assign' src/GeniusSDK.cpp  # confirm reset before append
rg -n 'std::string.*_(copy|)\(eth_private_key|std::string.*_(copy|)\(mnemonic' src/GeniusSDK.cpp  # confirm value capture
    </automated>
  </verify>
  <done>
A verification report section with 3 PER-FUNCTION checks (A/B/C) and 1 cross-function check (D), each recording PASS with line-number evidence. No FAIL results unless genuine defects exist.
</done>
</task>

<task type="auto">
  <name>Task 2: VER-02 Dead Reference Audit + VER-03 Doxygen Accuracy</name>
  <files>
    - .planning/phases/02-verification-documentation/02-VERIFICATION.md (appended)
  </files>
  <action>
Complete the verification report with VER-02 and VER-03 checks.

**VER-02 — Dead Reference Audit:**

Run these grep scans against src/GeniusSDK.cpp:

1. `grep -n 'NewFromPrivateKey' src/GeniusSDK.cpp` → expected: ZERO matches
2. `grep -n 'NewFromMnemonic' src/GeniusSDK.cpp` → expected: ZERO matches
3. `grep -n 'GeniusNode::New(' src/GeniusSDK.cpp` → expected: exactly 3 matches (lines 179, 207, 235), all using the new 2-arg `New(DevConfig_st, AccountSource{variant})` signature with no legacy params.
4. On match #3, verify each call site:
   - L179: `GeniusNode::New(cfg.value(), AccountSource{NewAccount{}})` — 2 args, no legacy params
   - L207: `GeniusNode::New(cfg.value(), AccountSource{FromPrivateKey{key_copy}})` — 2 args
   - L235: `GeniusNode::New(cfg.value(), AccountSource{FromMnemonic{mnemonic_copy}})` — 2 args
   No `autodht`, `process`, `baseport`, `is_full_node` visible in any call.

5. Broader scan for escaped references in comments/strings:
   `grep -ri 'NewFromPrivateKey\|NewFromMnemonic\|GeniusSDKInitWithKeyAndDevConfig\|GeniusSDKInitMinimal' src/` → expected: ZERO matches

Document each scan with: pattern searched, expected count, actual count, PASS/FAIL.

**VER-03 — Doxygen @param Accuracy:**

Extract Doxygen blocks for the 3 init functions from GeniusSDK.h and cross-reference with signatures:

1. `GeniusSDKInit` (L193-200):  
   - Signature: `GeniusSDKInit(const char *base_path, const char *dev_config)`  
   - @param tags: `base_path`, `dev_config` → 2 tags match 2 params ✓  
   - Check @returns text says "null on failure" → matches return type `const char *` ✓  
   - Verify NO tags reference: autodht, process, baseport, is_full_node
   - Verify NO tags reference removed functions (WithKeyAndDevConfig, Minimal)

2. `GeniusSDKInitWithKey` (L202-211):  
   - Signature: `GeniusSDKInitWithKey(const char *base_path, const char *dev_config, const char *eth_private_key)`  
   - @param tags: `base_path`, `dev_config`, `eth_private_key` → 3 tags match 3 params ✓  
   - Same stale-reference checks as above

3. `GeniusSDKInitWithMnemonic` (L213-222):  
   - Signature: `GeniusSDKInitWithMnemonic(const char *base_path, const char *dev_config, const char *mnemonic)`  
   - @param tags: `base_path`, `dev_config`, `mnemonic` → 3 tags match 3 params ✓  
   - Same stale-reference checks as above

For each function, note PASS/FAIL with line numbers. If any @param is missing, extra, or misnamed → note as FAIL with the specific remediation needed.

After all checks, append a summary table to the verification report:

```
| Requirement | Checks | Passed | Failed | Overall |
|-------------|--------|--------|--------|---------|
| VER-01      | 4      |        |        | PASS    |
| VER-02      | 5      |        |        | PASS    |
| VER-03      | 3      |        |        | PASS    |
```

If all checks pass: write `**Phase 2 — VERIFIED ✓**` with the total pass count.
If any check fails: write the specific remediation needed, apply the fix, and re-verify.
</action>
  <verify>
    <automated>
# VER-02 dead references
rg -c 'NewFromPrivateKey|NewFromMnemonic' src/GeniusSDK.cpp  # must be 0
rg -c 'GeniusSDKInitWithKeyAndDevConfig|GeniusSDKInitMinimal' src/GeniusSDK.cpp src/GeniusSDK.h  # must be 0
rg -c 'GeniusNode::New\(' src/GeniusSDK.cpp  # must be exactly 3

# VER-03 stale Doxygen params
rg -c '@param.*autodht|@param.*baseport|@param.*is_full_node|@param.*process' src/GeniusSDK.h  # must be 0 (but note: @param for jsondata in non-init functions will match "process" — filter those out)

# VER-03 Doxygen coverage
rg -B12 'GeniusSDKInit\(' src/GeniusSDK.h | rg '@param' | wc -l  # should find 8 @param tags across 3 init functions
    </automated>
  </verify>
  <done>
A complete 02-VERIFICATION.md with VER-01 (4 checks), VER-02 (5 scans), and VER-03 (3 function audits), all marked PASS with line-number citations. Summary table shows PASS across all 3 requirements. No unaddressed FAIL results remain.
</done>
</task>

<task type="auto">
  <name>Task 3: Finalize Verification Report + Update STATE.md</name>
  <files>
    - .planning/phases/02-verification-documentation/02-VERIFICATION.md (finalized)
    - .planning/STATE.md (updated)
  </files>
  <action>
Finalize the verification report and update project state.

**In 02-VERIFICATION.md:**
- Add a frontmatter block at the top:
  ```yaml
  ---
  phase: 02-verification-documentation
  verified: [current ISO 8601 timestamp]
  status: passed | failed (see summary table)
  requirements_verified: [VER-01, VER-02, VER-03]
  ---
  ```
- Add an executive summary paragraph stating: what was verified, total checks run, pass rate, and the overall disposition.
- If all 3 requirements pass: write "All 3 init functions preserve their return contract, contain zero dead references, and have accurate Doxygen documentation. Phase 2 verification is complete."
- If any requirement fails: include a "Remediation Required" section with exact steps.

**In STATE.md:**
- Find the `.gsd_state_version` frontmatter block
- Update `status` to `planning` (keep — this is the verification phase, not execution completion)
- Update `progress.completed_phases` from 0 to 1 (Phase 1 is done)  
- Update `progress.completed_plans` — check how many plans exist in Phase 1 (2 plans in ROADMAP) and add 1 for this plan = 3
- Update `progress.percent` accordingly
- In the "Current Position" section:
  - Update "Phase: 2 of 2 (Verification & Documentation)"
  - Update "Plan: 1 of 1 in current phase"  
  - Update "Status: Verified" if all checks pass
  - Update "Last activity" to the current date
- Update the progress bar and percentage
</action>
  <verify>
    <automated>
# Verify report frontmatter
python3 -c "import yaml; d=yaml.safe_load(open('.planning/phases/02-verification-documentation/02-VERIFICATION.md').read().split('---')[1]); assert d['phase']=='02-verification-documentation', 'bad phase'; print('frontmatter OK, status:', d.get('status'))"

# Verify STATE.md updated
python3 -c "import yaml; d=yaml.safe_load(open('.planning/STATE.md').read().split('---')[1]); assert d['progress']['completed_phases']>=1, 'phases not updated'; print('STATE OK, phases:', d['progress']['completed_phases'])"

# Verify report has summary section
rg -c '## Executive Summary|## Summary' .planning/phases/02-verification-documentation/02-VERIFICATION.md
    </automated>
  </verify>
  <done>
02-VERIFICATION.md has frontmatter, executive summary, and complete per-requirement check results. STATE.md reflects Phase 1 completion and Phase 2 verification status. Both files committed.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced in this phase — verification is read-only static analysis of existing source files.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Verification script output | accept | No PII, no secrets — verification report contains only grep counts and line numbers from public repository source |
</threat_model>

<verification>
**Phase-level verification — confirm all checks passed before declaring done:**

```bash
# Confirm VER-01: all 3 init functions have null-on-failure + path-on-success
rg -A30 '^const char \*GeniusSDKInit\(' src/GeniusSDK.cpp | rg 'return nullptr|return ret_val.c_str()' | wc -l
# Expected: at least 4 for Init, 5 for InitWithKey, 5 for InitWithMnemonic

# Confirm VER-02: zero dead factory method references anywhere
rg -c 'NewFromPrivateKey|NewFromMnemonic|GeniusSDKInitWithKeyAndDevConfig|GeniusSDKInitMinimal' src/GeniusSDK.cpp src/GeniusSDK.h
# Expected: 0 on both files

# Confirm VER-03: no stale @param tags in header
rg -c 'autodht|baseport|is_full_node' src/GeniusSDK.h
# Expected: 0
```
</verification>

<success_criteria>
- [ ] 02-VERIFICATION.md exists with frontmatter including `phase`, `verified`, `status`, `requirements_verified`
- [ ] VER-01 documented with per-function path tracing (4 checks) — all PASS
- [ ] VER-02 documented with 5 grep scans — all PASS (zero matches)
- [ ] VER-03 documented with 3 function Doxygen audits — all PASS (tags match signatures, zero stale references)
- [ ] Summary table shows PASS across all 12 checks
- [ ] STATE.md updated: completed_phases=1, current phase=2, status updated
- [ ] Git commit with both files
</success_criteria>

<output>
Create `.planning/phases/02-verification-documentation/02-01-SUMMARY.md` when done
</output>
